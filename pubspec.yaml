name: url_launcher_plus
description: Customizable url launcher plus with AppBar for navigation and also a refresh button available
version: 1.1.0
homepage: https://github.com/swarajkumarsingh/url_launcher_plus

environment:
  sdk: ">=3.0.3 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
